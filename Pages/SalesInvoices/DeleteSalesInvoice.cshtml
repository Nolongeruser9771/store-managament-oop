@page "/sales_invoices/delete"
@model store_managament_oop.Pages.SalesInvoices.DeleteSalesInvoiceModel
@{
	ViewData["Title"] = "Delete Sales Invoice";
}
<div class="container mt-5">
    <h1 class="mb-4">Delete Sales Invoice</h1>

    <!-- Confirmation Message -->
    <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">Are you sure?</h4>
        <p>You are about to delete the sales invoice <strong>@Model.salesInvoice.invoiceNumber</strong>.</p>
        <p>This action cannot be undone.</p>
        <hr>
        <p class="mb-0">If you are sure, click on "Delete Invoice". Otherwise, click on "Cancel" to go back.</p>
    </div>

    <!-- Delete Confirmation Form -->
    <form method="POST" class="mt-3">
        <!-- Action Buttons -->
        <button type="submit" class="btn btn-danger">Delete Invoice</button>
        <a href="/sales_invoices" class="btn btn-secondary ml-2">Cancel</a>
    </form>
    @Model.message
</div>